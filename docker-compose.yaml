name: repo
services:
  web:
    container_name: '${COMPOSE_PROJECT_NAME}_web'
    restart: always
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3333:3333"
    networks:
      - sail
include:
  - ./apps/web/docker-compose.yaml
